RewriteEngine On

# Block any URL that contains /public/ - return 403 Forbidden
RewriteRule ^public/ - [F,L]

# Redirect all other requests to public folder (internal)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ public/$1 [L]
